upctr - Loop optimizations in UPC

INSTALL.txt

--------------------------------

upctr has the following dependences:

ROSE >= 0.9.5a (rosecompiler.org)
GNU Autoconf >= 2.68
GNU Make >= 3.82
 

upctr builds with Autotools:
  ./configure [ --with-rose=/path/to/rose ]
  make
  make install
